#include "main.h"

/**
 * print lower case alphabets
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	char a;
	
	for (a = 'a'; c <= 'z'; a++)
		_putchar(a);
	_putchar('\n');

	return (0);
}
